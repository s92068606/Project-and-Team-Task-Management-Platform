{
  "name": "project-team-task-management-platform",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "lint": "npm run lint --workspace @cyphlab/api && npm run lint --workspace @cyphlab/web",
    "test": "npm run test --workspace @cyphlab/api",
    "build": "npm run build --workspace @cyphlab/api && npm run build --workspace @cyphlab/web",
    "dev": "npm run dev --workspace @cyphlab/api",
    "format": "npm run format --workspace @cyphlab/api && npm run format --workspace @cyphlab/web"
  }
}
